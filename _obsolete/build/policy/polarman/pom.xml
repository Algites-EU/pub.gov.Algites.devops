<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>eu.algites.tool.build</groupId>
	<artifactId>pub.gov.Algites.devops_build.policy.polarman</artifactId>
	<version>1.0.0-SNAPSHOT</version>
	<packaging>jar</packaging>

	<name>Algites Java Build - Policy Artifact Manager</name>
	<description>Manager allowing the build and creation of new policy artifacts.</description>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
	</properties>

	<build>
		<directory>${project.basedir}/run/bld/maven</directory>

		<sourceDirectory>src/product/java</sourceDirectory>
		<outputDirectory>run/product/classes</outputDirectory>

		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.13.0</version>
				<configuration>
					<source>17</source>
					<target>17</target>
					<showDeprecation>true</showDeprecation>
					<showWarnings>true</showWarnings>
				</configuration>
			</plugin>

<!--
			&lt;!&ndash; Copy and filter template to target/base-parent.pom &ndash;&gt;
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-resources-plugin</artifactId>
				<version>3.3.1</version>
				<executions>
					<execution>
						<id>filter-parent-pom</id>
						<phase>generate-resources</phase>
						<goals>
							<goal>copy-resources</goal>
						</goals>
						<configuration>
							<outputDirectory>${project.build.directory}</outputDirectory>
							<resources>
								<resource>
									<directory>src/product/maven</directory>
									<includes>
										<include>pom.xml.tpl</include>
									</includes>
									<filtering>true</filtering>
								</resource>
								<resource>
									<directory>src/product/gradle</directory>
									<includes>
										<include>plugin.kt.tpl</include>
									</includes>
									<filtering>true</filtering>
								</resource>
							</resources>
							<overwrite>true</overwrite>
						</configuration>
					</execution>
				</executions>
			</plugin>

			&lt;!&ndash; Attach generated POM as the project artifact &ndash;&gt;
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>build-helper-maven-plugin</artifactId>
				<version>3.5.0</version>
				<executions>
					<execution>
						<id>add-source-folders</id>
						<phase>package</phase>
						<goals>
							<goal>attach-artifact</goal>
						</goals>
						<configuration>
							<artifacts>
								<artifact>
									<file>${project.build.directory}/pom.xml.tpl</file>
									<type>pom</type>
								</artifact>
							</artifacts>
						</configuration>
					</execution>
				</executions>
			</plugin>
-->
		</plugins>
	</build>

	<distributionManagement>
		<snapshotRepository>
			<id>algites-public-releases-upload</id>
			<url>https://maven.cloudsmith.io/algites/maven-snapshots-pub/</url>
		</snapshotRepository>
		<repository>
			<id>cloudsmith</id>
<!--			<id>algites-public-snapshots-upload</id>-->
			<url>https://dl.cloudsmith.io/algites/maven-snapshots-pub/</url>
		</repository>
	</distributionManagement>
</project>
