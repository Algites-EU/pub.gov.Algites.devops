#Algites versions used to filter templates in build/basic.
# NOTE: keys are referenced from templates via:
#   @project.build.tool.version@
#   @version:<groupId>:<artifactId>@
#
# Artifact version (published coordinates) for build/basic outputs:
project.build.tool.version=1.0.0-SNAPSHOT
algites.build.base.pub.policy.version=2026.01

# Common dependency / plugin versions used inside templates (adjust as needed):
#version:org.junit.jupiter:junit-jupiter=5.10.2
#version:org.slf4j:slf4j-api=2.0.13
#version:org.apache.maven.plugins:maven-surefire-plugin=3.2.5